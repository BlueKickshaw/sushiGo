package Game;

import Cards.Card;
import java.util.*;

public class Deck {
    Stack<Card> deck;

    Deck(){
        // create an arraylist, shuffle it, the populate the stack
        ArrayList<Card> tmp;

        }
    }
}
